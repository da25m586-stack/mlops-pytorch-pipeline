model:
  architecture: resnet18
  num_classes: 10
  pretrained: false

data:
  data_dir: data/
  num_workers: 2

training:
  epochs: 50
  batch_size: 64
  learning_rate: 0.001
  early_stopping_patience: 10

output:
  checkpoint_dir: checkpoints/
  model_name: best_model.pt
